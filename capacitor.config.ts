import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flm.financialmanager',
  appName: 'FLM - Financial Life Manager',
  webDir: 'dist'
};

export default config;